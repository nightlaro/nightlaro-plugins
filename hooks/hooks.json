{
  "description": "Auto-capture context when tasks are completed",
  "hooks": {
    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/knowledge-bank-hook.sh"
          }
        ]
      }
    ]
  }
}
